---
title: 镜像源清单
---

## podman

```
sudo mkdir -p /etc/containers/registries.conf.d

sudo tee /etc/containers/registries.conf.d/docker-mirror.conf >/dev/null <<'EOF'
[[registry]]
prefix = "docker.io"
location = "docker.io"

[[registry.mirror]]
location = "docker.m.daocloud.io"
EOF
```

## github

```
git config --global url."https://v4.gh-proxy.org/https://github.com/".insteadOf "https://github.com/"
```

取消：

```
git config --global --unset-all \
url."https://v4.gh-proxy.org/https://github.com/".insteadOf
```

## ubuntu 26.04

```
sudo cp /etc/apt/sources.list.d/ubuntu.sources \
  /etc/apt/sources.list.d/ubuntu.sources.bak

sudo tee /etc/apt/sources.list.d/ubuntu.sources > /dev/null <<'EOF'
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/ubuntu
Suites: resolute resolute-updates resolute-backports
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

Types: deb
URIs: http://security.ubuntu.com/ubuntu/
Suites: resolute-security
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
EOF

sudo apt update
cat /etc/apt/sources.list.d/ubuntu.sources
```

恢复：

```
sudo cp /etc/apt/sources.list.d/ubuntu.sources.bak \
  /etc/apt/sources.list.d/ubuntu.sources
sudo apt update
```

## python uv

测阿里延迟

```
curl -L -o /dev/null \
  -w 'speed=%{speed_download} bytes/s total=%{time_total}s\n' \
  https://mirrors.aliyun.com/pypi/packages/source/n/numpy/numpy-2.3.3.tar.gz
```

测清华延迟

```
curl -L -o /dev/null \
  -w 'speed=%{speed_download} bytes/s total=%{time_total}s\n' \
  https://pypi.tuna.tsinghua.edu.cn/packages/source/n/numpy/numpy-2.3.3.tar.gz
```

哪个快用哪个，下面的代码里是用的阿里，想换成清华或华为就把第一条命令换成：

```
echo 'export UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple' >> ~/.bashrc
或
echo 'export UV_DEFAULT_INDEX=https://repo.huaweicloud.com/repository/pypi/simple/' >> ~/.bashrc

```

```
echo 'export UV_DEFAULT_INDEX=https://mirrors.aliyun.com/pypi/simple/' >> ~/.bashrc
echo 'export UV_PYTHON_INSTALL_MIRROR=https://v4.gh-proxy.org/https://github.com/astral-sh/python-build-standalone/releases/download' >> ~/.bashrc
source ~/.bashrc
```

有的项目会写死 uv 使用的源，需要用 --default-index 覆盖

```
uv pip install \
  --python .venv/bin/python \
  --default-index https://pypi.tuna.tsinghua.edu.cn/simple \
  -e "python[diffusion]" \
  --prerelease=allow
```

## huggingface

hf:

```
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
```

下载用 `hfd`（自带进度、断点续传，中断后**重跑即续传**）。

**`hfd` 的默认下载器是 `aria2c`（多连接）。** 机器上要先有这个包，否则会退回单连接的
`wget`，速度差一个数量级：

```
sudo apt-get install -y aria2
```

实测同一个源（`hf-mirror.com`）下 34 GB 的权重：

| 下载器 | 速度 | 34 GB 需要 |
| --- | --- | --- |
| `wget`（单连接） | 10–25 MB/s | 约 40 分钟 |
| `aria2c -x 4`（`hfd` 默认） | **80+ MB/s** | 几分钟 |

**另一个坑：不加 `--include` 会把整个仓库拉下来。** `hfd` 会先把仓库体积报出来——例如
`Comfy-Org/MiniMax-H3` 是 **480 GB**。只取其中一两个文件要写全仓库内的相对路径：

```
hfd Comfy-Org/MiniMax-H3 \
    --include "diffusion_models/minimax_h3_fl2va_int8_convrot.safetensors" \
    --local-dir /opt/orbis/models
```

`--local-dir` 下保持仓库内的相对路径，所以这行命令落地的是
`/opt/orbis/models/diffusion_models/minimax_h3_fl2va_int8_convrot.safetensors`——正好是
ComfyUI 认的目录结构，不用再手动搬。

**下完核对 sha256。** 远程头的 `x-linked-etag` 就是 LFS 文件的 sha256：

```
curl -sIL https://hf-mirror.com/<org>/<repo>/resolve/main/<文件路径> | grep -iE "x-linked-(etag|size)"
sha256sum <本地文件>
```

## rust

下载 rustup 镜像：

```
cat >> ~/.bashrc <<'EOF'
export RUSTUP_DIST_SERVER=https://rsproxy.cn
export RUSTUP_UPDATE_ROOT=https://rsproxy.cn/rustup
EOF

curl --proto '=https' --tlsv1.2 -sSf https://rsproxy.cn/rustup-init.sh | sh
```

cargo config

```
mkdir -p ~/.carg config

cat > ~/.cargo/config.toml <<'EOF'
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[registries.rsproxy]
index = "https://rsproxy.cn/crates.io-index"
EOF
```
