---
title: Markdown/MDX 速查表
description: Markdown 语法速查手册，篇幅缘故并没有列出
---

**粗体**、_斜体_、~~删除线~~。

这是一条脚注[^1]，后面还有一些额外的文字说明。

[^1]: 我的参考来源。

:::note[温馨提示]
这是一条提示信息

```sh
npm create astro@latest -- --template starlight
```

:::

:::tip[小技巧]
这是一个实用的小技巧
:::

:::tip[自定义图标]{icon="heart"}
[可用的图标](https://starlight.astro.build/reference/icons/#all-icons)
:::

:::caution
请注意
:::

:::danger
危险警告
:::

> 引用块，常用于引用他人的话或文档内容。
>
> 在每行开头使用 `>` 即可创建。

```js {2-3}
function demo() {
  // 这一行（第2行）和下一行会被高亮显示
  // 这一行（第2行）和下一行会被高亮显示
  return "这是代码片段的第3行";
}
```

```js "demo" /甚至.*呢/
// 单个词汇也可以高亮哦
function demo() {
  return "甚至支持正则表达式呢！";
}
```

```js "return true;" ins="插入" del="删除"
function demo() {
  console.log("这些是插入和删除的标记类型");
  // return 语句使用默认的标记类型
  return true;
}
```

```diff lang="js"
  function thisIsJavaScript() {
    // 整个块会按 JavaScript 语法高亮，
    // 同时我们还可以添加差异标记！
-   console.log('要删除的旧代码')
+   console.log('闪亮登场的新代码！')
  }
```

```js
// my-test-file.js
console.log("你好，世界！");
```

```bash title="安装依赖中…"
npm install
```

```bash frame="none"
echo "这段代码虽然用的是 bash 语言，但不会显示为终端样式"
```

<details>
<summary>仙女座星系什么时候在哪里最容易被观测到？</summary>

[仙女座星系](<https://zh.wikipedia.org/wiki/仙女座>)在 11 月份的夜空中最容易观测，可见纬度范围在 `+90°` 到 `−40°` 之间。

</details>


## 组件

import { Steps } from "@astrojs/starlight/components";
import { Card } from "@astrojs/starlight/components";

<Card title="来看看这个">你想突出的有趣内容。</Card>

<Steps>

1. 将组件导入到你的 MDX 文件中：

   ```js
   import { Steps } from "@astrojs/starlight/components";
   ```

2. 用 `<Steps>` 把你的有序列表包裹起来。

</Steps>

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
  <TabItem label="恒星">天狼星、织女星、参宿四</TabItem>
  <TabItem label="卫星">木卫一、木卫二、木卫三</TabItem>
</Tabs>

```d2
direction: right
installation -> configuration
```
