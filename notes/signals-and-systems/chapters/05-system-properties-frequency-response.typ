= 系统性质与频率响应

== 因果性

因果系统的当前输出只能依赖现在和过去的输入，不能依赖未来输入。

例如：

$ y(t) = x(t) + x(t-1) $

是因果系统，因为只使用当前输入和过去输入。

而：

$ y(t) = x(t+1) $

不是因果系统，因为当前输出需要知道未来 1 秒的输入。

对于 LTI 系统，因果性可以通过冲激响应判断：

$ h(t) = 0 quad "for" quad t < 0 $

直观上就是：系统在被冲激之前不能提前产生响应。

== 稳定性

稳定性可以先理解成：有限的输入不应该导致无限大的输出。

如果输入始终有界，而系统输出却不断增大到没有上限，那么这个系统是不稳定的。

== 频率响应

给 LTI 系统输入一个正弦波：

$ A sin(2 pi f t + phi) $

输出仍然具有相同的频率 $f$，但振幅和相位可能改变。

例如：

$ sin(2 pi 10 t) arrow 0.3 sin(2 pi 10 t + theta) $

频率仍然是 10 Hz。

因此，可以用不同频率的正弦波测试系统，记录每个频率被放大或衰减多少、相位改变多少。系统对所有频率的这种响应，就是频率响应。

频率响应回答的问题是：

- 哪些频率容易通过？
- 哪些频率会被衰减？
- 不同频率会产生多少相位变化？

例如，低通滤波器会较多保留低频，同时衰减高频。

傅里叶分析把复杂信号拆成很多不同频率的正弦成分，频率响应则描述系统如何分别处理这些频率成分。

因此可以把处理过程理解成：

复杂信号 → 分解成各个频率 → 系统分别处理 → 再叠加回来

== h(t) 与 H(f)

对于 LTI 系统，$h(t)$ 是系统的冲激响应，也就是系统在时域里的描述。

对 $h(t)$ 做傅里叶变换：

$ H(f) = cal(F) {h(t)} $

就得到系统的频率响应 $H(f)$。

因此：

$ h(t) arrow.l.r H(f) $

它们描述的是同一个 LTI 系统，只是观察角度不同。

$h(t)$ 回答的是：

*给系统一个瞬时冲激以后，系统随时间怎样响应？*

$H(f)$ 回答的是：

*不同频率进入系统后，会被放大或衰减多少，相位会改变多少？*

可以把它们先记成：

- $h(t)$：系统的时域说明书；
- $H(f)$：系统的频域说明书。

== 卷积定理

对于 LTI 系统，时域中的输入输出关系是：

$ y(t) = x(t) ast h(t) $

对等式两边做傅里叶变换，会得到：

$ Y(f) = X(f) H(f) $

因此：

$ x ast h arrow.l.r X H $

也就是说：

*时域里的卷积，等价于频域里的逐频率相乘。*

如果某个频率处：

$ H(f) approx 1 $

说明该频率基本保留。

如果：

$ H(f) approx 0 $

说明该频率被强烈衰减。

所以频率响应 $H(f)$ 可以理解成系统对每一个频率分别设置的“幅度旋钮 + 相位旋钮”。

== 一个简单的频域滤波例子

假设输入包含 100 Hz 和 1000 Hz 两个频率，并且它们的幅度都是 1。

若一个低通系统满足：

$ H(100) = 1 $

$ H(1000) = 0.2 $

那么经过系统后，100 Hz 基本保留，而 1000 Hz 只剩原来的 20%。

在频域里，这就是：

$ Y(f) = X(f) H(f) $

在时域里，同一件事写成：

$ y(t) = x(t) ast h(t) $

两种写法描述的是同一个系统作用过程。

== FIR 与 IIR

FIR 是 Finite Impulse Response，中文是“有限冲激响应”。

例如：

$ y[n] = x[n] + 0.5 x[n-1] $

它只依赖当前和过去的输入，对应的冲激响应是：

$ h[n] = [1, 0.5] $

冲激响应只有有限个非零值，所以叫 FIR。

IIR 是 Infinite Impulse Response，中文是“无限冲激响应”。

例如：

$ y[n] = x[n] + 0.8 y[n-1] $

这里当前输出依赖上一次输出，因此这是递归实现。

如果给这个系统一个单位冲激，它的冲激响应会持续下去：

$ h[n] = 1, 0.8, 0.8^2, 0.8^3, dots $

所以称为 IIR。

要注意：

*卷积本身不等于递归。*

FIR 可以直接通过卷积实现；IIR 虽然常用递归公式实现，但如果它是 LTI 系统，仍然可以从理论上写成：

$ y = x ast h $

只是它的 $h$ 会无限延续。