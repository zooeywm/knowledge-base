= 正弦波、周期、频率与相位

== 周期与频率

最基本的正弦波可以写成：

$ x(t) = sin(2 pi f t) $

其中 $f$ 是频率，单位是 Hz，表示每秒重复多少次。

如果 $f = 2$ Hz，就表示每秒重复 2 次。

周期 $T$ 表示完成一次重复需要多久：

$ T = 1 / f $

因此：

- $f$ 越大，变化越快。
- $T$ 越大，重复一次所需时间越长。

== 为什么有 2π

正弦函数走完一个完整周期，对应角度从 0 变化到 $2 pi$。

因此如果每秒需要重复 $f$ 次，角度每秒就要前进 $2 pi f$。

常把：

$ omega = 2 pi f $

称为角频率，所以正弦波也可以写成：

$ x(t) = sin(omega t) $

== 振幅与相位

更完整的正弦波形式是：

$ x(t) = A sin(2 pi f t + phi) $

其中：

- $A$ 是振幅，决定波形有多大。
- $f$ 是频率，决定波形变化有多快。
- $phi$ 是相位，决定波形从周期中的哪个位置开始。

可以先记成：

*振幅决定多大，频率决定多快，相位决定从哪里开始。*

== 相位与时间平移

如果把正弦波整体向右移动 $t_0$ 秒：

$ x(t-t_0) = sin(2 pi f (t-t_0)) $

展开后：

$ sin(2 pi f t - 2 pi f t_0) $

因此相位与时间平移满足：

$ phi = -2 pi f t_0 $

也就是说，相位变化可以理解成波形在时间轴上的平移。

一个完整周期 $T$ 对应相位变化 $2 pi$，所以：

- 半个周期 $T/2$ 对应 $pi$。
- 四分之一个周期 $T/4$ 对应 $pi/2$。
