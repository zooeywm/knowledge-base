= LTI、冲激响应与卷积

== 冲激响应

把单位冲激输入一个系统：

$ delta(t) arrow h(t) $

输出 $h(t)$ 叫做系统的冲激响应（Impulse Response）。可以把它理解成：给系统瞬间“敲一下”，观察系统之后怎样反应。

“系统”不一定是计算机程序，也可以是机械、电路、房间、传感器、滤波器等。分析时可以把它视为黑盒，只关心输入与输出。

== LTI 系统

LTI 是 Linear Time-Invariant，中文是“线性时不变”。

=== 什么是线性

线性的核心是两件事：保持加法，保持数乘。

如果系统记作 $T$，那么线性系统满足：

$ T(a x_1 + b x_2) = a T(x_1) + b T(x_2) $

也就是说，多个输入可以分开处理，再把各自输出相加；输入放大多少倍，输出也按同样倍数放大。

这与线性代数里的“线性”是同一个数学概念。区别只是线性代数通常处理向量，而信号与系统里处理的对象常常是一整个函数或序列。

例如：

$ y(t) = 2 x(t) $

是线性的。

而：

$ y(t) = x(t)^2 $

不是线性的，因为一般有：

$ (x_1 + x_2)^2 != x_1^2 + x_2^2 $

=== 什么是时不变

时不变表示：系统的行为不会因为输入发生的时间不同而改变。

如果：

$ x(t) arrow y(t) $

那么把输入整体延迟 $a$：

$ x(t-a) $

输出也只会整体延迟同样的时间：

$ y(t-a) $

例如，如果：

$ delta(t) arrow h(t) $

那么时不变系统一定满足：

$ delta(t-a) arrow h(t-a) $

=== 为什么“线性 + 时不变”特别重要

线性负责“能相加”，时不变负责“能平移复用”。

因此，如果已经知道：

$ delta(t) arrow h(t) $

那么就立即知道：

$ A delta(t-a) arrow A h(t-a) $

这使得一个冲激响应 $h(t)$ 可以被反复缩放和平移，用来构造系统对复杂输入的输出。

== 信号可以拆成冲激

离散信号可以写成许多不同位置、不同大小的冲激之和。

例如：

$ x[n] = 2 delta[n] + 5 delta[n-1] + 3 delta[n-2] $

连续信号也有同样的思想：普通信号可以理解成无数个不同时间位置的冲激叠加。

$ x(t) = integral_(-infinity)^(infinity) x(tau) delta(t-tau) dif tau $

这里 $tau$ 只是积分时使用的临时时间变量。

== 为什么出现 h(t-tau)

如果一个冲激发生在 $t=0$，响应是 $h(t)$。

如果这个冲激晚 $tau$ 时刻发生，因为系统时不变，响应也整体晚 $tau$：

$ h(t-tau) $

所以 $x(tau) h(t-tau)$ 表示：在 $tau$ 时刻大小为 $x(tau)$ 的输入，产生的一份缩放并延迟后的响应。

== 卷积从哪里来

对于 LTI 系统，每一个输入冲激都会产生一份平移并缩放后的冲激响应。把所有这些响应加起来，就是最终输出。

离散时间：

$ y[n] = sum_(k=-infinity)^(infinity) x[k] h[n-k] $

连续时间：

$ y(t) = integral_(-infinity)^(infinity) x(tau) h(t-tau) dif tau $

记作：

$ y(t) = x(t) ast h(t) $

因此在 LTI 系统这个场景里，可以把卷积直观理解成：

*把每个输入点产生的冲激响应全部叠加起来。*

教材中常见的“翻转、平移、相乘、求和”是卷积公式的图形计算方法，不是它最本质的物理含义。

== 卷积本身并不要求冲激信号

卷积本身只是两个函数或两个序列之间的一种数学运算，并不天然等于“用冲激探测黑盒”。

更一般地，可以写成：

$ (f ast g)(t) = integral_(-infinity)^(infinity) f(tau) g(t-tau) dif tau $

这里的 $f$ 和 $g$ 都可以是普通函数或普通信号，并不要求其中一个是冲激信号。

在 LTI 系统里，$h$ 之所以特殊，是因为它恰好是系统的冲激响应，所以：

$ y = x ast h $

可以用来计算系统对输入 $x$ 的输出。

因此要区分三件事：

- $delta(t)$：可以用来探测一个 LTI 系统；
- $h(t)$：探测后得到的冲激响应，它描述这个 LTI 系统；
- $x ast h$：利用这个系统特性计算输出。

卷积也可以出现在没有“黑盒系统”或没有冲激信号的场景中。

== 当前输出是否依赖上一个输出

卷积本身不要求当前输出依赖上一个输出。

例如设：

$ h[n] = [1, 0.5] $

那么：

$ y[7] = x[7] h[0] + x[6] h[1] $

也就是：

$ y[7] = x[7] + 0.5 x[6] $

这里 $y[7]$ 确实依赖过去的输入 $x[6]$，因为过去输入产生的影响还没有消失。

但它并不依赖 $y[6]$。

所以更准确的说法是：

*如果冲激响应持续多个时刻，那么当前卷积输出会依赖当前和过去的输入。*

只有递归系统才可能直接依赖过去输出，例如：

$ y[n] = 0.8 y[n-1] + x[n] $

这种式子中的 $y[n]$ 才明确依赖 $y[n-1]$。

== 一个最简单的离散卷积

设：

$ x[n] = [1, 2] $

$ h[n] = [1, 1] $

第一个输入点 $x[0]=1$ 产生响应：

$ [1, 1, 0] $

第二个输入点 $x[1]=2$ 晚一个时刻发生，产生响应：

$ [0, 2, 2] $

叠加后：

$ y[n] = [1, 3, 2] $

因此完整卷积的结果长度为：

$ N + M - 1 $

其中 $N$ 和 $M$ 是两个有限序列的长度。

直观上，结果变长是因为每个输入点都会“拖出”一段 $h[n]$。如果系统的冲激响应持续多个时刻，那么最后一个输入结束后，系统还可能继续有余波。

== 有限序列与定义域

理论上的离散时间信号通常定义在整个整数轴：

$ n in ZZ $

实际计算时往往只观察有限一段，或者默认有限序列之外的值全部为零。

所以“序列点数更多”表示输入持续时间更长或观测区间更长，并不自动意味着卷积更准确。

== 连续卷积为什么用积分

离散时间中，我们把每一个输入点产生的响应用求和加起来：

$ sum_k x[k] h[n-k] $

连续时间中，时间位置无限密集，因此“连续地求和”就变成积分：

$ integral x(tau) h(t-tau) dif tau $

所以可以把积分理解成连续版本的求和。
