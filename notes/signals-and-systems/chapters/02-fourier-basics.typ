= 时域、频域与傅里叶变换

== 时域

时域关注的是：信号在不同时间点的值是什么。

如果一个信号写成 $x(t)$，横轴通常是时间 $t$，纵轴是信号值。

时域最擅长回答：

- 什么时候出现变化？
- 什么时候振幅变大或变小？
- 信号随时间是什么形状？

可以先记成：

*时域 = 看“什么时候发生了什么”。*

== 频域

频域关注的是：一个复杂信号由哪些频率组成，每个频率有多强。

例如，一个复杂声音可能同时包含 100 Hz、440 Hz 和 1000 Hz。

因此可以这样理解：

- 时域：什么时候发生了什么。
- 频域：里面包含哪些频率，以及每种频率有多强。

同一个信号可以写成时域形式 $x(t)$，也可以写成频域形式 $X(f)$。它们不是两个不同的信号，而是同一个信号的两种观察方式。

== 傅里叶变换在做什么

傅里叶变换把同一个信号从时域表示转换成频域表示。

它的核心思想是：复杂信号可以拆成许多不同频率的正弦波和余弦波。

因此：

时域信号 → 傅里叶变换 → 频域表示

频域表示 → 傅里叶逆变换 → 时域信号

可以把傅里叶变换先理解成“频率分解器”。

== 它怎么判断有没有某个频率

直观上，可以把傅里叶分析理解成：拿不同频率的标准波形和原信号逐个做匹配。

例如，要判断一个信号里有没有 100 Hz，就拿 100 Hz 的正弦波与原信号比较。如果同频成分很强，长期累加后的匹配结果会比较大；如果频率不匹配，正负贡献会大量互相抵消。

因此可以先理解成：

*傅里叶变换会对不同频率逐个询问“你在原信号里有多少”。*

== 为什么还需要余弦

只用一个正弦波还不够，因为同一个频率可以有不同相位。

例如：

$ sin(2 pi f t) $

和：

$ cos(2 pi f t) $

频率相同，只是相位相差四分之一个周期。

任意相位的同频正弦波都可以由同频的正弦和余弦组合出来：

$ A sin(omega t + phi) = A cos(phi) sin(omega t) + A sin(phi) cos(omega t) $

所以同时观察“正弦方向”和“余弦方向”，就能同时获得：

- 这个频率有多强；
- 这个频率的相位是多少。

这也是后面欧拉公式会自然出现的原因：它可以把正弦和余弦统一写在一个复指数里。

== 傅里叶公式的直觉

连续时间傅里叶变换写成：

$ X(f) = integral_(-infinity)^(infinity) x(t) e^(-i 2 pi f t) dif t $

这里 $f$ 表示当前正在检测的频率。

$e^(-i 2 pi f t)$ 可以理解为一个以 $f$ Hz 旋转的反向复数探针。把它与原信号相乘并在时间上累加：

- 如果原信号里存在同频成分，旋转会被抵消，累加结果较大；
- 如果频率不同，结果仍会旋转，长期累加时会大量互相抵消。

因此，对固定的一个 $f$，傅里叶变换得到一个频谱值 $X(f)$；把所有频率的结果放在一起，就得到整个频域表示 $X(f)$。

== 幅度谱与相位谱

$X(f)$ 通常是复数，因此它同时包含两个信息：

- 幅度：这个频率成分有多强；
- 相位：这个频率成分在一个周期里的位置。

幅度谱通常写成：

$ abs(X(f)) $

横轴是频率 $f$，纵轴表示不同频率成分的大小。

相位谱通常写成：

$ angle(X(f)) $

横轴仍然是频率，纵轴表示每个频率对应的相位。

例如：

$ x(t) = 2 cos(2 pi 100 t) + cos(2 pi 300 t + pi / 4) $

可以直观理解为：

- 100 Hz：幅度为 2，相位为 0；
- 300 Hz：幅度为 1，相位为 $pi / 4$，也就是 45°。

== 相位的物理意义

相位不是纯抽象概念。对于周期信号，它表示“当前处在一个周期的什么位置”。

一般正弦信号可以写成：

$ x(t) = A cos(2 pi f t + phi) $

其中 $phi$ 就是相位。

相位与时间偏移直接对应。若：

$ x(t) = cos(2 pi f (t - t_0)) $

展开可得：

$ x(t) = cos(2 pi f t - 2 pi f t_0) $

所以：

$ phi = -2 pi f t_0 $

也就是：

$ t_0 = - phi / (2 pi f) $

因此，相位差可以理解成两个同频周期信号之间的时间错位。

例如 100 Hz 的周期是 10 ms。若相位相差 90°，也就是四分之一个周期，则时间差为 2.5 ms。

== 实际计算为什么需要一段时间

理论上的傅里叶变换可以讨论整个时间轴，但真实计算机只能拿到有限长度的数据。

假设我们观察信号的总时长是 $T$，那么频率分辨率大致为：

$ Delta f = 1 / T $

所以观察时间越长，通常越容易区分两个非常接近的频率。

如果采样率是 $f_s$，一共采样 $N$ 个点，则：

$ T = N / f_s $

因此：

$ Delta f = f_s / N $
