= 欧拉公式、DFT 与 FFT

== 角度、theta 与弧度

$theta$（theta）通常用来表示角度。

角度既可以用“度”表示，也可以用“弧度”表示。

弧度的定义是：

$ theta = s / r $

其中 $s$ 是圆弧长度，$r$ 是半径。

因为整圆周长为：

$ 2 pi r $

所以整圆对应的弧度为：

$ (2 pi r) / r = 2 pi $

因此：

$ 360 degree = 2 pi $

$ 180 degree = pi $

$ 90 degree = pi / 2 $

$ 45 degree = pi / 4 $

角度换算：

$ "弧度" = "度数" times pi / 180 $

如果用度数 $alpha$ 表示圆心角，弧长为：

$ s = 2 pi r times alpha / 360 $

如果用弧度 $theta$ 表示，则因为 $theta=s/r$，所以：

$ s = r theta $

== 复数与虚数单位 i

普通实数中，没有一个数的平方等于 $-1$。

因此定义虚数单位：

$ i^2 = -1 $

形如：

$ a + b i $

的数叫复数。

复数可以看成二维平面上的点：实部 $a$ 是横坐标，虚部系数 $b$ 是纵坐标。

例如：

$ 3 + 2 i $

对应平面上的点 $(3, 2)$。

== 乘 i 的几何意义

在复平面上，乘一次 $i$ 等价于逆时针旋转 $90 degree$。

因为：

$ 1 arrow i arrow -1 arrow -i arrow 1 $

每一步都相当于再旋转四分之一圈。

所以可以先把 $i$ 理解成一个“旋转 $90 degree$ 的操作”。

== 单位圆与 sin、cos

半径为 $r$、角度为 $theta$ 的圆上点可以写成：

$ x = r cos(theta) $

$ y = r sin(theta) $

因此：

$ P = (r cos(theta), r sin(theta)) $

当 $r=1$ 时得到单位圆：

$ P = (cos(theta), sin(theta)) $

复数 $a+bi$ 对应二维坐标 $(a,b)$，所以单位圆上的同一个点也可以写成：

$ cos(theta) + i sin(theta) $

它表示“从正实轴出发，逆时针旋转 $theta$ 后的位置”。

== 常数 e

$e$ 是一个特殊常数：

$ e approx 2.71828 $

它经常出现在连续增长问题中，例如：

$ (1 + 1/n)^n $

当 $n$ 越来越大时，会越来越接近 $e$。

指数函数 $e^x$ 有一个重要性质：它的变化速度等于它自己。

== 欧拉公式

欧拉公式是：

$ e^(i theta) = cos(theta) + i sin(theta) $

右边 $cos(theta)+i sin(theta)$ 表示单位圆上旋转 $theta$ 后的位置。

而 $e^(i theta)$ 也描述同一个旋转，因此两者相等。

可以先把它直观地记成：

$ e^(i theta) = "在复平面上旋转" theta $

当角度为负数时，就是顺时针旋转：

$ e^(-i theta) = cos(theta) - i sin(theta) $

== 它为什么会出现在傅里叶变换里

傅里叶分析需要不断比较“某个频率的正弦波和余弦波”与原信号有多匹配。

欧拉公式可以把正弦和余弦合并成一个复指数，所以傅里叶公式经常出现类似：

$ e^(-i 2 pi f t) $

其中：

- $f$ 是频率，单位通常是 Hz；
- $t$ 是时间；
- $2 pi f t$ 是随时间不断增加的角度；
- 负号表示在复平面上顺时针旋转。

因此这个复指数可以理解成一个以频率 $f$ 不断旋转的“标准探针”，用来检测原信号中是否包含这个频率。

== DFT

DFT 的全称是 Discrete Fourier Transform，中文是离散傅里叶变换。

它处理的是有限长度的离散数据：

$ x[0], x[1], dots, x[N - 1] $

DFT 把这些时域采样点转换成一组离散的频域结果。

它的公式是：

$ X[k] = sum_(n=0)^(N-1) x[n] e^(-i 2 pi k n / N) $

现在不需要背这个公式。先只记住它的意义：

*拿一段离散采样数据，分析里面有哪些离散频率成分。*

== N、n 与 k

$N$ 表示这次拿来做 DFT 的采样点总数。

例如：

$ x[0], x[1], dots, x[7] $

一共有 8 个采样点，所以：

$ N = 8 $

$n$ 表示“当前正在处理第几个时域采样点”，因此：

$ n = 0, 1, 2, dots, N - 1 $

$k$ 表示“当前正在检查第几个频率格子”，同样有：

$ k = 0, 1, 2, dots, N - 1 $

所以可以记成：

- $N$：总共有多少个采样点；
- $n$：第几个时间采样点；
- $k$：第几个频率格子。

== 频率格子

如果采样率是 $f_s$，那么第 $k$ 个频率格子对应：

$ f_k = k f_s / N $

相邻两个频率格子的间隔是：

$ Delta f = f_s / N $

因为观察总时长：

$ T = N / f_s $

所以也可以写成：

$ Delta f = 1 / T $

这说明：观察时间越长，频率格子越密，频率分辨率越高。

例如：

$ f_s = 48000 " Hz" $

如果取 1 秒数据：

$ N = 48000 $

那么：

$ Delta f = 1 " Hz" $

所以频率格子可以理解为：0 Hz、1 Hz、2 Hz、3 Hz……

== DFT 如何匹配每个频率

在公式：

$ X[k] = sum_(n=0)^(N-1) x[n] e^(-i 2 pi k n / N) $

里，复指数：

$ e^(-i 2 pi k n / N) $

就是第 $k$ 个频率对应的反向旋转探针。

对于固定的 $k$，DFT 会让 $n$ 从 0 到 $N-1$，把整段时域采样逐点与这个探针相乘，然后全部累加。

因此：

$ X[k] $

可以理解为“第 $k$ 个频率与整段数据的匹配结果”。

DFT 会让 $k$ 从 0 到 $N-1$ 全部算一遍，所以并不需要事先知道原信号里有哪些频率。

== DFT 的输出是什么

DFT 输入是一个离散时域数列：

$ x[0], x[1], dots, x[N-1] $

输出也是一个长度为 $N$ 的数列：

$ X[0], X[1], dots, X[N-1] $

但 $X[k]$ 通常是复数。

每一个 $X[k]$ 对应一个频率格子，并同时包含：

- 这个频率成分的幅度；
- 这个频率成分的相位。

因此可以概括为：

*DFT：时域数列 $x[n]$ → 频域复数数列 $X[k]$。*

== FFT

FFT 的全称是 Fast Fourier Transform，中文是快速傅里叶变换。

FFT 不是另一种新的傅里叶变换。

它是“快速计算 DFT 的算法”。

直接计算 DFT 大约需要 $N^2$ 量级的工作，而典型 FFT 可以把工作量降到约 $N log N$。

因此可以记成：

*DFT 是数学变换，FFT 是高效计算 DFT 的算法。*

== 傅里叶逆变换

傅里叶变换把时域信号拆成不同频率成分：

$ x(t) arrow X(f) $

傅里叶逆变换则把这些频率成分重新叠加，恢复时域信号：

$ X(f) arrow x(t) $

连续傅里叶逆变换为：

$ x(t) = integral_(-infinity)^(infinity) X(f) e^(i 2 pi f t) dif f $

直觉上，$X(f)$ 给出每个频率的幅度和相位，$e^(i 2 pi f t)$ 生成该频率的旋转波，最后把所有频率成分叠加起来。

因此可以记成：

- 傅里叶变换：拆频率；
- 傅里叶逆变换：合频率。

== IDFT 与 IFFT

计算机里的离散版本是：

$ x[n] arrow X[k] $

这一步叫 DFT；反方向：

$ X[k] arrow x[n] $

叫 IDFT（Inverse Discrete Fourier Transform，离散傅里叶逆变换）。

对应地：

- FFT：快速计算 DFT；
- IFFT：快速计算 IDFT。

所以计算机里的完整闭环可以写成：

$ x[n] arrow.r("DFT / FFT") X[k] arrow.r("IDFT / IFFT") x[n] $

理想数学条件下，先做 DFT/FFT，再做 IDFT/IFFT，可以恢复原来的那串离散时域采样值。

这里要注意：

*IDFT 恢复的是离散时域 $x[n]$，并不是直接恢复连续模拟信号 $x(t)$。*
