= 积分与冲激信号

== 积分的直觉

现阶段可以先把积分理解成：

*把很多很小的贡献累加起来。*

例如：

$ integral_0^3 2 dif t = 6 $

因为函数值始终为 2，从 0 到 3 的面积就是：

$ 2 times 3 = 6 $

因此定积分可以直观理解为“带正负号的面积累加”。

横轴上方的面积记为正，横轴下方的面积记为负。

== 单位冲激信号

单位冲激信号写成：

$ delta(t) $

可以把它理解成发生在某一个瞬间的理想化冲击。

它满足：

$ delta(t) = 0, quad t != 0 $

同时：

$ integral_(-infinity)^(infinity) delta(t) dif t = 1 $

需要注意：等于 1 的是冲激的总面积，而不是说 $delta(0)=1$。

== 冲激和平移

$ delta(t-a) $

表示冲激发生在 $t=a$。

例如：

$ delta(t-3) $

表示在 3 秒时发生一个单位冲激。

== 冲激与阶跃的关系

如果从负无穷开始累积冲激：

$ integral_(-infinity)^t delta(tau) dif tau = u(t) $

也就是说，在经过冲激之前累计结果为 0，经过冲激之后累计结果为 1。

== 筛选性质

冲激函数最重要的性质之一是：

$ integral_(-infinity)^(infinity) x(t) delta(t-a) dif t = x(a) $

可以把 $delta(t-a)$ 理解成一根“取样针”：它让积分只保留 $t=a$ 附近的贡献。

由于单位冲激的总面积是 1，所以最后得到：

$ x(a) times 1 = x(a) $

例如，如果：

$ x(t)=2t $

那么：

$ integral_(-infinity)^(infinity) 2t delta(t-3) dif t = 6 $

现阶段可以先记住：

*积分 = 累加；冲激 = 瞬时探针；冲激配合积分可以取出函数在某一个时刻的值。*
