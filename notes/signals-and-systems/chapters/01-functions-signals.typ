= 函数与信号

== 函数是什么

函数可以先理解成一个规则：给它一个输入，它按照固定规则给出一个输出。

例如：

$ y = 2 x $

当 $x = 1$ 时，$y = 2$；当 $x = 3$ 时，$y = 6$。

也可以写成：

$ y = f(x) $

这里的 $f$ 表示“从输入 $x$ 得到输出 $y$ 的规则”。

== 信号是什么

在信号与系统里，一个最常见的信号写成：

$ x(t) $

这里不是 $x$ 乘以 $t$。它表示：$x$ 是一个函数，输入是时间 $t$。

因此，$x(t)$ 可以读成：“信号 $x$ 在时间 $t$ 时的值”。

例如：

$ x(t) = 2 t $

那么：

- $x(0) = 0$
- $x(1) = 2$
- $x(2) = 4$

很多物理量都随时间变化，例如声音、传感器电压、温度。因此可以先记住：

*信号通常就是一个随时间变化的函数。*

== 连续时间信号与离散时间信号

连续时间信号通常写成：

$ x(t) $

这里的时间 $t$ 可以取任意实数，例如 $0$、$0.1$、$0.123$、$1$。

离散时间信号通常写成：

$ x[n] $

这里的 $n$ 通常只取整数：$0, 1, 2, 3, dots$。

因此：

- $x(t)$：连续时间信号。
- $x[n]$：离散时间信号。

== 采样

现实世界中的声音、电压等通常先表现为连续信号 $x(t)$。计算机处理时，需要按固定时间间隔取样，把它变成离散序列 $x[n]$。

如果每隔 $T_s$ 秒采一次，则：

$ x[n] = x(n T_s) $

这里 $T_s$ 叫做*采样间隔*。

采样频率记作 $f_s$：

$ f_s = 1 / T_s $

例如 $T_s = 0.001$ 秒，则：

$ f_s = 1000 " Hz" $

意思是每秒采 1000 个点。

== ADC

ADC 是 Analog-to-Digital Converter，中文叫*模数转换器*。

它负责把连续的模拟电压变成计算机能够保存和处理的数字数据。

可以先把它理解成两步：

- *采样*：隔一段时间测一次电压。
- *量化*：把测到的电压映射到有限的数字等级。

因此常见的数据链路是：

模拟信号 → ADC → 数字序列

反过来，把数字信号变回模拟电压的设备叫 DAC（Digital-to-Analog Converter，数模转换器）。

== 信号的时间平移

$ x(t - a) $

表示把原信号向右移动 $a$。

例如 $x(t - 1)$ 表示向右移动 1 秒。

而：

$ x(t + a) $

表示向左移动 $a$。

最容易搞反，所以先记：

- 减号：右移。
- 加号：左移。

== 信号的时间翻转

$ x(-t) $

表示沿时间轴左右翻转。

例如原来位于 $t = 2$ 的特征，会移动到 $t = -2$。

注意不要和下面这个混淆：

$ -x(t) $

- $x(-t)$：左右翻。
- $-x(t)$：上下翻。

== 信号的时间缩放

$ x(2t) $

表示时间轴压缩 2 倍。原来出现在 $t = 2$ 的特征会出现在 $t = 1$。

而：

$ x(t / 2) $

表示时间轴拉伸 2 倍。原来出现在 $t = 2$ 的特征会出现在 $t = 4$。

因此可以先记：

- $x(2t)$：更快。
- $x(t / 2)$：更慢。

== 单位阶跃信号

单位阶跃信号写成：

$ u(t) $

它表示一个在 $t = 0$ 打开的理想开关：

- 当 $t < 0$ 时，$u(t) = 0$。
- 当 $t >= 0$ 时，$u(t) = 1$。

如果写成：

$ u(t - a) $

它就在 $t = a$ 时打开。

例如：

$ u(t - 2) $

表示到 2 秒时才从 0 变成 1。

== 用阶跃信号拼出矩形脉冲

考虑：

$ x(t) = u(t) - u(t - 2) $

分三段看：

- $t < 0$：$0 - 0 = 0$。
- $0 <= t < 2$：$1 - 0 = 1$。
- $t >= 2$：$1 - 1 = 0$。

因此它表示一个从 $t = 0$ 开始、到 $t = 2$ 结束的矩形脉冲。

更一般地：

$ u(t - a) - u(t - b) $

可以理解为：在 $t = a$ 时打开，在 $t = b$ 时关闭。
