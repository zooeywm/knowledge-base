= 采样、Nyquist 与混叠

== 从连续信号到离散样本

现实中的模拟信号通常写成 $x(t)$。

ADC 不会保存所有连续时刻，而是按照固定采样率 $f_s$ 读取一串离散值：

$ x[0], x[1], x[2], dots $

采样率 $f_s$ 的单位是 Hz，表示“每秒采多少次”。

例如：

$ f_s = 48000 "Hz" $

表示每秒采 48000 个样本。

采样间隔为：

$ T_s = 1 / f_s $

== Nyquist 频率

Nyquist（奈奎斯特）是人名。

对于采样率 $f_s$，Nyquist 频率定义为：

$ f_N = f_s / 2 $

例如：

$ f_s = 48000 "Hz" $

则：

$ f_N = 24000 "Hz" $

理想情况下，为了无歧义地表示最高频率为 $f_max$ 的信号，需要满足：

$ f_s > 2 f_max $

可以先记成：

*采样率至少要高于最高信号频率的两倍。*

== 为什么采样率太低会出问题

如果采样太慢，一个高频波形可能只留下很少几个点。

这些采样点可能和另一个更低频率的波形完全一样。

这时，仅从采样结果已经无法判断原来的真实频率。

== Aliasing：混叠

Aliasing 中文通常叫“混叠”。

它表示：

*不同的真实连续频率，在采样后产生了相同或无法区分的离散样本。*

例如采样率为：

$ f_s = 8 "Hz" $

Nyquist 频率为：

$ 4 "Hz" $

一个 7 Hz 的余弦信号在这些采样时刻可能产生和 1 Hz 余弦信号相同的样本。

因此可以直观地理解成：

*7 Hz 在采样后“冒充”成了 1 Hz。*

混叠通常是坏的，因为一旦发生，原始频率信息可能已经不可逆地丢失。

== 抗混叠滤波器

为了防止超过 Nyquist 频率的成分混叠到低频区域，ADC 前通常会使用低通滤波器。

它的目标是：

- 低频尽量保留；
- 高频逐渐衰减；
- 在采样前尽量压低 Nyquist 频率以上的成分。

这种用途的低通滤波器叫 anti-aliasing filter，也就是抗混叠滤波器。

== RC 低通滤波器

最简单的模拟低通滤波器之一是 RC 电路。

它由电阻 $R$ 和电容 $C$ 组成。

其截止频率为：

$ f_c = 1 / (2 pi R C) $

直觉上：电容对高频更容易导通，因此高频成分更容易被旁路到地，而低频更多地保留在输出端。

如果把这个电路近似为 LTI 系统，那么：

$ y(t) = x(t) ast h(t) $

在频域则是：

$ Y(f) = X(f) H(f) $

低通滤波器的 $H(f)$ 在低频处较大，在高频处较小，所以高频分量会被压低。
